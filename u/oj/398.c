#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char* compress(char*);

int main(){
    char user[100] = "";
    scanf("%s", user);
    
    int len = strlen(user);

    for(int i = 0; i < len; i++){
        
    }
}

char* compress(char* src){

}